app:
  log_level: error
  log_output: file

cluster:
  name: dev-cluster
  node_count: 1

serve:
  host: localhost
  port: 9092
  workers: 2

connect:
  endpoint: yaml.example.com:9002
  timeout_ms: 1000
  retry:
    count: 2
    interval_ms: 100
